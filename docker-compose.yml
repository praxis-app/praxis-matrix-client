version: '3.9'

services:
  app:
    container_name: praxis-matrix-client
    build:
      context: .
      target: runtime_stage
      args:
        - NODE_ENV=${NODE_ENV}
        - PORT=${PORT}
    ports:
      - ${PORT}:${PORT}
    restart: always
    env_file: .env
