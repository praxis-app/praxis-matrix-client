import { Button } from './components/ui/button';

const App = () => {
  return (
    <div className="flex h-screen w-full items-center justify-center">
      <Button className="font-inter">Praxis Matrix Client</Button>
    </div>
  );
};

export default App;
