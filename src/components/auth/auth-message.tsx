// TODO: Add welcome message

import { BotMessage } from '../messages/bot-message';

export const AuthMessage = () => {
  return (
    <BotMessage>
      <div>Welcome to Praxis!</div>
    </BotMessage>
  );
};
